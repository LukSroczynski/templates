buildscript {
    repositories { mavenCentral() }
    dependencies { classpath("org.springframework.boot:spring-boot-gradle-plugin:1.5.4.RELEASE") }
}

allprojects {
    apply plugin: 'application'
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }
}

project(":DesignPattern-Factory") {}

project(":DesignPattern-Singleton") {}


